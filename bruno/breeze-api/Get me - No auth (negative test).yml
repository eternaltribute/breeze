info:
  name: Get me - No auth (negative test)
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/auth/me"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
