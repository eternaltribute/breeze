info:
  name: Create Interview
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/jobs/ba72ec7d-b663-4549-9498-3a2063c15872/interviews"
  body:
    type: json
    data: |-
      {
        "interview_round": "Technical Screen",
        "interview_datetime": "2026-07-01T14:00:00",
        "notes": "Focus on system design and Python"
      }
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
