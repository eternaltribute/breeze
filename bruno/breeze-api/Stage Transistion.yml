info:
  name: Stage Transistion
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{base_url}}/jobs/ba72ec7d-b663-4549-9498-3a2063c15872/stage"
  body:
    type: json
    data: |-
      {
        "new_stage": "offer",
        "confirm_override": true
      }
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
