info:
  name: Stage Transistion
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{base_url}}/jobs/98c6be81-510a-4114-b03a-bcfdcf0926f3/stage"
  body:
    type: json
    data: |-
      {
        "new_stage": "offer",
        "confirm_override": true
      }
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
