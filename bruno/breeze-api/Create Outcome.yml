info:
  name: Create Outcome
  type: http
  seq: 12

http:
  method: POST
  url: "{{base_url}}/jobs/98c6be81-510a-4114-b03a-bcfdcf0926f3/outcome"
  body:
    type: json
    data: |-
      {
        "notes":"Offer accepted, starting August 1"
      }
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
