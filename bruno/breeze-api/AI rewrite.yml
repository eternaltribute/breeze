info:
  name: AI rewrite
  type: http
  seq: 18

http:
  method: POST
  url: "{{base_url}}/jobs/{job_id}/ai/rewrite"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
